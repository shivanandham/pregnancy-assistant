# Backend URL Configuration
# Development backend URL (used when in debug mode)
BACKEND_URL_DEV=http://192.168.0.8:3000

# Production backend URL (used when in release mode)
BACKEND_URL_PROD=https://lumacare.in
